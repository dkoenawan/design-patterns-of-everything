---
import BaseLayout from '../layouts/BaseLayout.astro';
import Hero from '../components/Hero.astro';
import DomainCard from '../components/DomainCard.astro';
import Starfield from '../components/Starfield';
---

<BaseLayout
  title="Solution Architect Portfolio | Design Patterns & Architecture Experience"
  description="Design patterns, architecture experience, and real-world case studies across frontend, backend, data pipeline, and infrastructure domains."
>
  <Starfield client:load />

  <!-- Hero Section -->
  <Hero />

  <hr class="constellation-divider" />

  <!-- About Section -->
  <section class="about">
    <div class="container">
      <h2>Mapping the Architecture Landscape</h2>
      <div class="about-content">
        <p class="body-lg">
          Software architecture is a vast, interconnected system of knowledge. This portfolio maps that territory the way a cartographer charts constellations—documenting proven patterns, charting connections between domains, and marking the routes that lead to successful systems.
        </p>
        <p class="body-lg">
          Each pattern represents a point of light: tested in production, understood through experience, connected to related concepts. Skill trees visualize how expertise develops across domains, from foundational patterns to advanced architectural decisions. The map expands as new territories are explored.
        </p>
        <p class="body-lg">
          Whether you're evaluating architectural depth, assessing problem-solving approaches, or navigating your own journey through system design, these charts provide reference points drawn from real implementations—not theoretical abstractions.
        </p>
      </div>
    </div>
  </section>

  <hr class="constellation-divider" />

  <!-- Expertise Areas / Domains Section -->
  <section class="domains" id="domains">
    <div class="container">
      <h2>Four Regions of the Map</h2>
      <p class="domains-intro body-lg">
        Architecture expertise divided into four domains, each a distinct territory with its own patterns, challenges, and skill progressions. Navigate between them to see how patterns connect across boundaries.
      </p>
      <div class="domains-grid">
        <DomainCard
          domain="frontend"
          title="Frontend Architecture"
          description="Component patterns, state management strategies, micro-frontends, and scalable UI systems for complex applications."
        />
        <DomainCard
          domain="backend"
          title="Backend Architecture"
          description="Clean architecture, CQRS, hexagonal patterns, and API design principles for maintainable server-side systems."
        />
        <DomainCard
          domain="data"
          title="Data Pipeline Architecture"
          description="Medallion architecture, schema-driven validation, pure functional pipelines, and reliable data transformation patterns."
        />
        <DomainCard
          domain="infra"
          title="Infrastructure & Platform"
          description="Microservices orchestration, container platforms, infrastructure as code, and CI/CD pipeline design."
        />
      </div>
    </div>
  </section>

  <hr class="constellation-divider" />

  <!-- Approach Section -->
  <section class="approach">
    <div class="container">
      <h2>How This Map Was Drawn</h2>
      <div class="approach-grid">
        <div class="approach-card">
          <h3>Charted Through Experience</h3>
          <p>
            Every pattern documented here solved a real problem in a production system. Complexity ratings reflect actual implementation challenges. Case studies show the terrain as it was encountered—constraints, trade-offs, outcomes.
          </p>
        </div>
        <div class="approach-card">
          <h3>Organized by Territory</h3>
          <p>
            Architecture knowledge naturally clusters into domains. Frontend, backend, data pipelines, and infrastructure each have distinct patterns and principles. The map respects these boundaries while showing where they connect.
          </p>
        </div>
        <div class="approach-card">
          <h3>Built to Be Navigated</h3>
          <p>
            Skill trees visualize progression paths. Patterns link to related concepts. Anti-patterns mark known hazards. The structure reveals not just what to know, but how understanding develops across a career.
          </p>
        </div>
      </div>
    </div>
  </section>

  <hr class="constellation-divider" />

  <!-- Contact Section -->
  <section class="contact">
    <div class="container">
      <h2>Connect</h2>
      <p class="body-lg">
        This portfolio is actively evolving. More patterns, case studies, and interactive visualizations coming soon.
      </p>
      <p class="contact-note">
        Professional inquiries and collaboration opportunities welcome.
      </p>
    </div>
  </section>
</BaseLayout>

<style>
  /* About Section */
  .about {
    background: var(--color-surface);
  }

  .about-content {
    max-width: 800px;
    margin: 0 auto;
  }

  /* Domains Section */
  .domains {
    padding: var(--space-3xl) 0;
  }

  .domains-intro {
    text-align: center;
    max-width: 700px;
    margin: 0 auto var(--space-2xl);
    color: var(--color-text-secondary);
  }

  .domains-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: var(--space-xl);
    max-width: 1000px;
    margin: 0 auto;
  }

  @media (min-width: 768px) {
    .domains-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  /* Approach Section */
  .approach {
    background: var(--color-surface);
  }

  .approach-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-xl);
    max-width: 1000px;
    margin: 0 auto;
  }

  @media (min-width: 768px) {
    .approach-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .approach-card {
    padding: var(--space-lg);
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-left: 3px solid var(--color-secondary);
    border-radius: var(--radius-md);
    box-shadow: var(--shadow-md);
    transition: all var(--transition-normal);
  }

  .approach-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
    border-left-color: var(--color-accent);
  }

  .approach-card h3 {
    color: var(--color-secondary);
    margin-bottom: var(--space-sm);
  }

  .approach-card p {
    color: var(--color-text-secondary);
    line-height: var(--leading-relaxed);
  }

  /* Contact Section */
  .contact {
    text-align: center;
  }

  .contact-note {
    margin-top: var(--space-lg);
    color: var(--color-text-muted);
    font-style: italic;
  }

  /* Common Section Styles */
  section h2 {
    text-align: center;
    margin-bottom: var(--space-xl);
    color: var(--color-secondary);
    text-shadow: 0 2px 8px hsla(40, 60%, 65%, 0.2);
  }

  /* Constellation Divider */
  .constellation-divider {
    border: none;
    height: 1px;
    background: var(--color-border);
    position: relative;
    margin: var(--space-3xl) auto;
    max-width: 600px;
  }

  .constellation-divider::after {
    content: '✦';
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    color: var(--color-secondary);
    background: var(--color-bg);
    padding: 0 var(--space-md);
    font-size: var(--text-body-lg);
    text-shadow: 0 0 8px hsla(40, 60%, 65%, 0.4);
  }
</style>
