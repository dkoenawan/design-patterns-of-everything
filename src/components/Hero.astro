---
// Hero section component
---

<section class="hero">
  <div class="constellation-bg">
    <svg class="constellation-map" aria-hidden="true">
      <!-- Central constellation connecting 4 domains -->
      <!-- Frontend node (top-left) -->
      <circle cx="20%" cy="30%" r="4" class="star star-frontend" />
      <!-- Backend node (top-right) -->
      <circle cx="80%" cy="30%" r="4" class="star star-backend" />
      <!-- Data node (bottom-left) -->
      <circle cx="25%" cy="70%" r="4" class="star star-data" />
      <!-- Infrastructure node (bottom-right) -->
      <circle cx="75%" cy="70%" r="4" class="star star-infra" />

      <!-- Connecting lines (constellation pattern) -->
      <line x1="20%" y1="30%" x2="80%" y2="30%" class="constellation-line" />
      <line x1="20%" y1="30%" x2="25%" y2="70%" class="constellation-line" />
      <line x1="80%" y1="30%" x2="75%" y2="70%" class="constellation-line" />
      <line x1="25%" y1="70%" x2="75%" y2="70%" class="constellation-line" />
      <line x1="20%" y1="30%" x2="75%" y2="70%" class="constellation-line diagonal" />
      <line x1="80%" y1="30%" x2="25%" y2="70%" class="constellation-line diagonal" />

      <!-- Scattered ambient stars -->
      <circle cx="10%" cy="15%" r="2.5" class="ambient-star" opacity="0.6" />
      <circle cx="90%" cy="20%" r="2" class="ambient-star" opacity="0.5" />
      <circle cx="15%" cy="85%" r="1.5" class="ambient-star" opacity="0.7" />
      <circle cx="85%" cy="80%" r="1.5" class="ambient-star blue" opacity="0.4" />
      <circle cx="50%" cy="10%" r="2" class="ambient-star blue" opacity="0.5" />
      <circle cx="45%" cy="90%" r="2" class="ambient-star" opacity="0.6" />
      <circle cx="5%" cy="50%" r="1.5" class="ambient-star" opacity="0.5" />
      <circle cx="95%" cy="55%" r="1.5" class="ambient-star" opacity="0.6" />
      <circle cx="35%" cy="25%" r="1.5" class="ambient-star blue" opacity="0.5" />
      <circle cx="65%" cy="55%" r="2" class="ambient-star" opacity="0.7" />
      <circle cx="12%" cy="60%" r="1" class="ambient-star blue" opacity="0.4" />
      <circle cx="88%" cy="45%" r="1.5" class="ambient-star" opacity="0.5" />
      <circle cx="55%" cy="75%" r="1" class="ambient-star" opacity="0.6" />
      <circle cx="30%" cy="40%" r="1.5" class="ambient-star blue" opacity="0.6" />
    </svg>
  </div>

  <div class="container hero-content">
    <h1 class="display">Navigate the architecture landscape</h1>
    <p class="body-lg hero-subtitle">
      A constellation of design patterns mapped across four domains. Each node represents proven solutions,
      hard-won experience, and the connections between architectural decisions. Like a star chart for software systems.
    </p>
    <a href="#domains" class="btn btn-primary">Begin exploration →</a>
  </div>
</section>

<style>
  .hero {
    position: relative;
    padding: var(--space-3xl) 0;
    text-align: center;
    overflow: hidden;
    background: transparent;
  }

  .constellation-bg {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    pointer-events: none;
    opacity: 0.15;
    max-width: 800px;
    max-height: 400px;
    margin: auto;
  }

  .constellation-map {
    width: 100%;
    height: 100%;
  }

  /* Domain-colored stars */
  .star {
    filter: drop-shadow(0 0 4px currentColor);
    animation: pulse 3s ease-in-out infinite;
  }

  .star-frontend {
    fill: var(--color-domain-frontend);
  }

  .star-backend {
    fill: var(--color-domain-backend);
  }

  .star-data {
    fill: var(--color-domain-data);
  }

  .star-infra {
    fill: var(--color-domain-infra);
  }

  .ambient-star {
    fill: var(--color-secondary);
    filter: drop-shadow(0 0 2px var(--color-secondary));
    animation: star-pulse 5s ease-in-out infinite;
  }

  .ambient-star.blue {
    fill: hsl(220, 80%, 85%);
    filter: drop-shadow(0 0 2px hsl(220, 80%, 85%));
  }

  @keyframes star-pulse {
    0%, 100% {
      opacity: 0.5;
    }
    50% {
      opacity: 1;
    }
  }

  /* Constellation connecting lines */
  .constellation-line {
    stroke: var(--color-secondary);
    stroke-width: 1;
    opacity: 0.4;
    filter: none;
  }

  .constellation-line.diagonal {
    opacity: 0.2;
  }

  @keyframes shimmer {
    0%, 100% {
      opacity: 0.7;
    }
    50% {
      opacity: 1;
    }
  }

  @keyframes pulse {
    0%, 100% {
      opacity: 1;
      transform: scale(1);
    }
    50% {
      opacity: 0.7;
      transform: scale(0.9);
    }
  }

  .hero-content {
    position: relative;
    z-index: 1;
  }

  .hero .display {
    max-width: 900px;
    margin: 0 auto var(--space-lg);
    color: var(--color-secondary);
    text-shadow: 0 2px 8px hsla(40, 60%, 65%, 0.3);
  }

  .hero-subtitle {
    max-width: 700px;
    margin: 0 auto var(--space-xl);
    color: var(--color-text-secondary);
    text-shadow: none;
  }

  .btn-primary {
    background: var(--color-accent);
    color: white;
    padding: var(--space-md) var(--space-xl);
    font-size: var(--text-body-lg);
    font-weight: var(--weight-semibold);
    border-radius: var(--radius-md);
    box-shadow: var(--shadow-md);
    transition: all var(--transition-normal);
    position: relative;
  }

  .btn-primary:hover {
    background: var(--color-accent-600);
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
  }

  @media (max-width: 640px) {
    .hero {
      padding: var(--space-2xl) 0;
    }

    .constellation-map {
      opacity: 0.5;
    }

    /* Simplify constellation on mobile */
    .constellation-line.diagonal {
      display: none;
    }
  }
</style>
